{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SFML App",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/sfml-app.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "externalConsole": true,
            "preLaunchTask": "Build SFML App"
        }
    ]
}
